# AI Dev Workflow 과제 로드맵

> AI 개발 워크플로우를 체계화하고 저장소 단위의 헌법, 도구(MCP), 코드 리뷰 및 관측, 캡스톤 운영 루프까지 단계별로 구축하는 4주 완성 과제 목록입니다.

---

## 📅 주차별 요약

| 주차 | 주제 | 주간 통합 PR 목표 | 핵심 기술 / 키워드 |
| :--- | :--- | :--- | :--- |
| **W1** | **컨텍스트와 프로젝트 헌법** | claude.md 헌법과 Hook 안전망 구축하기 | `claude.md`, Skills, PreToolUse, PostToolUse Hook |
| **W2** | **MCP와 Git 협업 자동화** | 이슈에서 검증된 PR까지 gh CLI로 한 바퀴 돌리기 | MCP, gh CLI, Git 브랜치 전략, Playwright |
| **W3** | **AI 코드리뷰와 관측** | 컨테이너화와 관측을 붙이고 리뷰 자동화를 실습하기 | Docker, Sentry, AI Code Review, GitHub Actions |
| **W4** | **캡스톤 운영 루프** | PRD에서 병합까지 캡스톤 한 바퀴 완주하기 | PRD, 이슈 트래킹, E2E 통합 운영, 회고 |

---

## 📌 W1. 컨텍스트와 프로젝트 헌법

> **목표**: `claude.md`와 스킬, PreToolUse·PostToolUse Hook으로 AI의 작업 품질을 저장소에 고정합니다.

### 🎯 주간 통합 PR
- **`claude.md 헌법과 Hook 안전망 구축하기`**

### ✅ 과제 체크리스트

#### [필수 과제]
- [x] **프로젝트 헌법 작성**: 저장소 루트의 `claude.md`에 프로젝트 구조, 실행 명령, 코딩 규칙, 금지 변경, 완료 조건을 이 저장소에서 실제로 통하는 경로와 명령으로 작성합니다.
  - 강의 6강의 헌법 작성 프롬프트를 활용하되, 생성된 문장이 실제 파일 구조와 일치하는지 직접 확인합니다.
- [x] **반복 작업 자동화 스킬 생성**: `.claude/skills/<이름>/SKILL.md`로 반복 작업을 대신하는 스킬 1개를 만듭니다.
  - 프론트매터의 `name`과 `description`을 채우고, 해당 스킬로 실제 산출물(예: `app/blog/posts/` 하위 새 글)이 생성되는 것을 확인합니다.
- [x] **PreToolUse Hook 안전망 구축**: `.claude/settings.json`에 PreToolUse Hook을 등록해 금지된 변경을 차단합니다.
  - 일부러 위반하는 요청을 넣어 차단되는 출력을 로그로 남깁니다. (강의 7강의 exit code 규약 준수)
- [x] **PostToolUse Hook 자동화 검사**: PostToolUse Hook으로 파일 수정 직후 포맷 또는 린트/검사가 자동 실행되도록 연결하고 실행 로그를 남깁니다.

#### [선택 확장 과제]
- [x] **지능형 접근 제어 확장**: 강의 8강의 dispatcher 패턴을 적용하여 경로·확장자별 분기를 만들고 지능형 접근 제어까지 확장합니다.

### 📦 제출 증거
1. 저장소 루트의 `claude.md`
2. `.claude/skills/<이름>/SKILL.md` 및 해당 스킬로 생성된 실제 산출물
3. `.claude/settings.json` (PreToolUse / PostToolUse Hook 설정 내역)
4. PreToolUse 위반 차단 출력 로그 및 PostToolUse 자동 포맷/검사 실행 로그
5. 근거형 질문 1~3 답변

### ❓ 함께 답할 근거형 질문
1. `claude.md`에 반드시 있어야 하지만 일반 `README.md`에는 없어도 되는 정보는 무엇인가요?
2. PreToolUse로 막아야 하는 변경과 PostToolUse로 뒤처리하면 되는 변경을 어떻게 구분하여 나누었나요?
3. Hook을 적용했음에도 막지 못한 실수는 무엇이었으며, 이를 향후 어떻게 개선하여 막을 계획인가요?

---

## 📌 W2. MCP와 Git 협업 자동화

> **목표**: MCP로 AI의 감각을 확장하고, 이슈에서 PR까지 gh CLI 협업 루프를 E2E 테스트로 완성합니다.

### 🎯 주간 통합 PR
- **`이슈에서 검증된 PR까지 gh CLI로 한 바퀴 돌리기`**

### ✅ 과제 체크리스트

#### [필수 과제]
- [x] **MCP 연동 및 권한 경계 설정**: 강의 2파트 1강의 필수 MCP 중 1개를 현재 공식 프로젝트 경로인 `.mcp.json`에 연결하고, 해당 MCP가 실제로 응답한 출력을 남깁니다.
  - 연결한 도구의 읽기·쓰기 범위와 사람 승인이 필요한 지점을 함께 명시합니다.
- [x] **이슈 기반 기능 구현 및 PR 발행**: 저장소에 기능 이슈 1건을 등록하고, `claude.md`에 브랜치·커밋·PR 규칙을 추가합니다.
  - `feature/<이슈번호>-<슬러그>` 브랜치에서 기능을 구현한 뒤 PR을 생성합니다.
  - `.github/PULL_REQUEST_TEMPLATE.md` 형식을 충실히 채워 제출합니다.
- [x] **E2E 테스트 시나리오 고정**: 기능의 동작을 `tests/` 하위 Playwright 시나리오 1개로 고정합니다.
  - 구현 전 커밋의 실패(Red)와 현재 구현의 통과(Green) 출력 로그를 모두 남깁니다. 과거 커밋 재검증과 당시 TDD 실행 기록을 구분합니다.

#### [선택 확장 과제]
- [x] **서브에이전트 및 병렬 작업**: `.claude/agents/` 서브에이전트를 정의하거나, `git worktree`로 두 번째 작업을 병렬로 진행하며 충돌 방지 경계를 기록합니다.

완료 근거와 제품별 검증 범위: [W2 증거 인덱스](docs/evidence/w2/README.md).

### 📦 제출 증거
1. 연결한 MCP 설정 내역, 실제 응답 출력, 읽기·쓰기 범위 및 사람 승인 지점 정리
2. 이슈 번호, feature 브랜치명, PR 템플릿을 채운 PR 링크
3. `tests/` 하위 Playwright 시나리오 1개 및 구현 전 실패 / 구현 후 통과 출력
4. 근거형 질문 1~3 답변

### ❓ 함께 답할 근거형 질문
1. 연결한 MCP에서 읽기 전용으로 충분한 지점과 쓰기 권한이 필요한 지점을 어떤 기준으로 나누었나요?
2. `claude.md`에 브랜치·커밋·PR 규칙을 명시하기 전과 후, AI의 작업 결과물과 협업 방식이 어떻게 달라졌나요?
3. 작성한 Playwright 시나리오가 이 기능의 무엇을 보장하며, 반대로 무엇은 여전히 보장하지 못하나요?

---

## 📌 W3. AI 코드리뷰와 관측

> **목표**: GitHub Actions 리뷰 워크플로우와 컨테이너화, 에러 관측 체계를 구축하여 병합 전 신뢰 경계를 만듭니다.

### 🎯 주간 통합 PR
- **`컨테이너화와 관측을 붙이고 리뷰 자동화를 실습하기`**

### ✅ 과제 체크리스트

#### [필수 과제]
- [x] **애플리케이션 컨테이너화**: `Dockerfile`로 앱을 컨테이너화하고, 컨테이너 환경에서 앱이 정상적으로 응답하는 실행 출력을 남깁니다.
- [x] **에러 관측(Sentry) 연동 및 지표 수립**: Sentry를 `app/` 코드에 연결하고 의도적으로 에러를 발생시켜 Sentry 대시보드에 에러가 수집되는 것을 확인합니다.
  - 운영 중 가장 먼저 살펴볼 핵심 지표와 구체적인 알람/경보 숫자 기준을 정의합니다.
- [x] **AI 코드리뷰 워크플로우 실습**: 강의 3파트 1강의 AI 코드리뷰를 개인 저장소에서 실습하고, AI 리뷰 코멘트가 달린 실행 링크를 증거로 남깁니다.
  - *참고: 본 과제 저장소의 `.github/workflows/`는 채점 보호 경로이므로, 워크플로우 파일 내용은 `docs/`에 정리하여 제출합니다.*

#### [선택 확장 과제]
- [ ] **클라우드 인프라 배포**: Terraform으로 AWS 인프라를 구성하거나 ECS에 배포합니다. (실제 비용이 발생할 수 있으므로 리소스 정리 내역까지 기록)
- [ ] **LLM 추적 관측**: Langfuse를 연동하여 LLM 호출 추적(Tracing) 로그를 남깁니다.

진행 증거: [W3 검증 인덱스](docs/evidence/w3/README.md), [관측 기준](docs/w3-observability.md), [리뷰 명세](docs/w3-review-workflow.md).

### 📦 제출 증거
1. `Dockerfile` 및 컨테이너 정상 응답 실행 출력
2. Sentry 연동 확인 내역 및 운영 관측 지표/경보 기준 문서
3. 개인 저장소 AI 코드리뷰 코멘트 실행 링크 및 `docs/` 내 워크플로우 명세 파일
4. 근거형 질문 1~3 답변

### ❓ 함께 답할 근거형 질문
1. AI 리뷰어가 제공한 피드백 중 사람이 미처 놓쳤을 만한 유의미한 지적은 무엇이었나요?
2. 단위/통합 테스트는 통과하지만 실제로는 병합되면 안 되는 위험한 변경을 이 AI 리뷰 설정이 걸러낼 수 있나요?
3. 운영 환경에서 문제를 가장 빠르고 정확하게 드러낼 수 있는 핵심 지표와 경보 기준은 무엇인가요?

---

## 📌 W4. 캡스톤 운영 루프

> **목표**: 기획에서 PRD, 티켓 발행, 구현 및 병합, 관측과 회고까지 전체 개발·운영 사이클을 하나의 루프로 완주합니다.

### 🎯 주간 통합 PR
- **`PRD에서 병합까지 캡스톤 한 바퀴 완주하기`**

### ✅ 과제 체크리스트

#### [필수 과제]
- [ ] **PRD(제품 요구사항 정의서) 작성**: 블로그에 추가할 기능 1개를 선정하고, `docs/prd.md`에 문제 정의, 성공 기준, 사용자 스토리, 기능·비기능 요구사항, 비범위(Out of Scope)를 표준 형식으로 작성합니다.
- [ ] **실행 단위 이슈 분할 및 추적**: 작성된 PRD를 작업 가능한 최소 실행 단위 이슈들로 쪼개고, 각 이슈에 명확한 인수 조건을 작성합니다.
  - 이슈 번호와 인수 조건이 PR 본문과 상호 추적되도록 연결합니다.
- [ ] **전체 파이프라인 관통 구현 및 병합**: 분할된 기능 중 1개를 1~3주차에 구축한 `claude.md`·Hook·리뷰 워크플로우를 모두 거쳐 구현 및 병합(Merge)까지 완료합니다.
  - `tests/` 하위 시나리오 테스트로 인수 조건을 검증 및 고정합니다.
- [ ] **운영 및 자동화 회고록 작성**: `docs/retrospective.md`에 지난 4주간 자동화가 실제로 줄여준 병목, 새롭게 야기한 병목, 그리고 다른 팀원이 저장소를 재사용하기 위해 제거해야 할 개인 의존성을 상세히 기록합니다.

#### [선택 확장 과제]
- [ ] **로컬 전체 스택 및 CI/CD 배포**: `docker-compose`로 전체 로컬 스택 구동 환경을 구성하거나 CI/CD를 통한 배포 자동화를 완성합니다.

### 📦 제출 증거
1. `docs/prd.md` (기능 명세 및 요구사항 정의서)
2. 인수 조건이 포함된 이슈 및 이를 추적하는 병합 완료된 PR 링크
3. `tests/` 하위의 인수 조건 검증 테스트 시나리오
4. `docs/retrospective.md` (4주간의 운영 및 워크플로우 회고 문서)
5. 근거형 질문 1~3 답변

### ❓ 함께 답할 근거형 질문
1. PRD에 정의한 성공 기준 중 이번 구현 사이클을 통해 실제로 확인된 것과 아직 확인하지 못한 것은 각각 무엇인가요?
2. 1~3주차에 구축한 헌법 규칙, Hook, 코드리뷰 워크플로우 중 이번 구현 과정에서 실제로 도움이 된 요소와 오히려 방해가 된 요소는 무엇인가요?
3. 동료 팀원이 이 저장소와 워크플로우를 그대로 인계받아 사용하려면, 현재 상태에서 추가로 문서화하거나 제거해야 할 부분은 무엇인가요?
