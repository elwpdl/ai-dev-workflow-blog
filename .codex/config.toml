# 공통 스킬은 .agents/skills, 프로젝트 지침은 AGENTS.md에서 읽습니다.
[features]
hooks = true

[mcp_servers.repo-github]
command = "bash"
args = ["-c", 'exec bash "$(git rev-parse --show-toplevel)/scripts/github-mcp.sh"']
env_vars = ["GITHUB_TOKEN", "GITHUB_PERSONAL_ACCESS_TOKEN"]
