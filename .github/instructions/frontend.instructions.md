# Frontend Instructions

- React 컴포넌트는 항상 Functional Component로 작성합니다.
- CSS는 Tailwind CSS 혹은 Global CSS (`app/globals.css`)를 사용합니다.
- RSC(React Server Components)를 기본으로 하며, 필요한 경우 상단에 `"use client"`를 선언합니다.
